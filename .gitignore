node_modules/
dist/
out/
# Generated by `npm run make:icon` from src/desktop/tray-icons.ts. Derived, so
# it is not committed — a checked-in binary and the script that makes it drift.
build/
.research/
# Hand-probe scripts. Anything worth keeping becomes a scripts/probe-*.ts that
# asserts rather than prints; the throwaway version should not outlive the hour.
.scratch/
*.log
.env
.env.*
!.env.example
.vite/
.turbo/
coverage/
*.tsbuildinfo
runtime-data/
.venv/
models/

# Python bytecode from the voice sidecar
__pycache__/
*.pyc

# Temporary generated vite configs
electron.vite.config.*.mjs

